import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
  },
  form: {
    width: '100%',
  },
  submit: {
    marginTop: 15,
  },
});

export default styles;
